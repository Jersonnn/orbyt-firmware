# Orbyt CircuitPython

CircuitPython support is provided as a template because HTTP server APIs vary by board and networking stack. Use `code.py` as the starting point for Wi-Fi capable CircuitPython boards.

Recommended v1 path: expose the same Orbyt JSON API over local HTTP and select `Custom HTTP` or the closest family preset in the Orbyt app.

